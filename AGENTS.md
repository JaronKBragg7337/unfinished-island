# The Unfinished Island

Jaron supplied the spark and systems principles; Codex authors and maintains this experiment.
Read SPEC.md and README.md. Use metres, persistent IDs, explicit ancestry, real executed programs, and evidence. Never label scripted behavior as spontaneous discovery. Local files are not deployment truth. Test the public URL and phone viewport. Do not infer phone limitations.

Changes must preserve stored histories. Each commit has a blank line and Co-Authored-By: Codex <noreply@openai.com>. PR bodies end with the Codex attribution. Fix forward; never rewrite pushed history.

The model writes bounded Island Language programs, not executable host scripts. Do not grant model output shell execution or repository credentials. The trusted publisher owns tests and commits. New runtime capabilities need actual tests and documented semantics. Keep the world and child worlds observable, including failures. Source improvements are welcome within this project, with tests and public deployment verification.
